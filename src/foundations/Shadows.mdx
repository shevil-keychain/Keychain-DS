import { Meta } from '@storybook/addon-docs/blocks';
import { ShadowSample, Sheet } from './components/ScaleSample';
import resolveConfig from 'tailwindcss/resolveConfig';
import tailwindConfig from '../../tailwind.config';

export const theme = resolveConfig(tailwindConfig).theme;

<Meta title="Foundations/Shadows" />

# Shadows

<Sheet>
  {Object.entries(theme.boxShadow).map(([token, value]) => (
    <ShadowSample
      key={token}
      token={`shadow${token === 'DEFAULT' ? '' : `-${token}`}`}
      value={String(value)}
    />
  ))}
</Sheet>
